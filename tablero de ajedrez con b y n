#include <iostream>
using namespace std;

int main() {
    for (int fila = 0; fila < 8; fila++) {
        for (int col = 0; col < 8; col++) {
            cout << ((fila + col) % 2 == 0 ? 'b' : 'n') << " ";
        }
        cout << endl;
    }
    return 0;
}
